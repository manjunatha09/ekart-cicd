package com.ekart;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class EkartApplicationTests {

    // A minimal context-load test. This is enough for the Jenkins "Unit
    // Tests" stage to have something real to run against; add more tests
    // as the application grows.
    @Test
    void contextLoads() {
    }
}
