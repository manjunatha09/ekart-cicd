spring.application.name=ekart

# The pipeline exposes the container on 8070 (see Dockerfile and
# kubernetes/deployment.yaml) so the app must listen on the same port.
server.port=8070

# Enables GET /actuator/health for Kubernetes liveness/readiness probes.
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
